import { CATEGORY } from "../../constants";

const categories = [
  CATEGORY.COMMON,
  CATEGORY.NOTICE,
  CATEGORY.PROJECT,
  CATEGORY.CODE_REVIEW,
  CATEGORY.FOUND,
  CATEGORY.LOST,
];

export default categories;
